<%= form_with(model: idea, local: true) do |form| %>
  <% if idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

      <ul>
      <% idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :idea_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :idea_description %>
  </div>

  <div class="field">
    <%= form.label :estimated_date %>
    <%= form.date_select :estimated_date, id: :idea_estimated_date %>
  </div>

  <div class="field">
    <%= form.label :goal_amount %>
    <%= form.number_field :goal_amount, id: :idea_goal_amount %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :idea_category %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :idea_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
